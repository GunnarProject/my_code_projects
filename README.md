# my_code_projects
ML Engineer Course

## Git commands:
* git clone: 
* git add
* git commit
    * 'commit with message:' git commit -m 'this is a message' 
* git push

### Git Branches

* Shows all the branches available: `git branch`
* Switch to the develop branch: `git checkout develop`
* Creates a branch as well as moves onto it: `git checkout -b`
* Pull the latest changes in the develop branch: `git pull`
* How to delete a branch: `git branch -d` 
* Push from local machine to github: `git push`